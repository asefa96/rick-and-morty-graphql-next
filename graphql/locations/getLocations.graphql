query {
  locations {
    info {
      count
    }
    results {
      id
      name
      type
      dimension
      residents {
        id
        name
        species
        status
        origin {
          name
        }
      }
    }
  }
}
